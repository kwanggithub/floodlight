This command allows a hashed password to be associated with the user.   The hashed password
has a specific format, create and managed by the REST API, using arbitrary text to set the
hashed-password will make it impossible for the user to log in.
