treespaces:
  - name: NestedListTest
    module_search_paths:
      - path: src/test/resources/net/bigdb/service/internal
        recursive: true
    data_source_mappings:
      - data_source: "$data-source"
        predicate: "!Config"
    modules:
      - name: NestedListTest
auth_config:
    enabled: false

